<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link href="/tailwind.css" rel="stylesheet"> <!-- Link to Tailwind CSS -->
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">

    <div class="w-full max-w-lg bg-white p-8 rounded-lg shadow-md">
        <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">Edit Post</h1>

        <!-- Post ID and Username Display -->
        <div class="mb-4">
            <p class="text-sm text-gray-600"><strong>Post ID:</strong> <%= post.id %></p>
            <p class="text-sm text-gray-600"><strong>Username:</strong> <%= post.username %></p>
        </div>

        <!-- Form to edit the post -->
        <form method="POST" action="/posts/<%= post.id %>?_method=PATCH" class="space-y-6">
            <div>
                <label for="content" class="block text-sm font-medium text-gray-700">Edit Content</label>
                <textarea 
                    name="content" 
                    rows="6" 
                    class="mt-1 p-2 w-full border border-gray-300 rounded-md focus:ring-blue-500 focus:border-blue-500"
                    required
                ><%= post.content %></textarea>
            </div>

            <!-- Submit Button -->
            <div class="text-center">
                <button 
                    type="submit" 
                    class="w-full bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50"
                >
                    Submit Changes
                </button>
            </div>
        </form>
    </div>

</body>
</html>
