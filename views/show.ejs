<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post Details</title>
    <link href="/tailwind.css" rel="stylesheet"> <!-- Link to Tailwind CSS -->
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">

    <!-- Card for post details -->
    <div class="w-full max-w-md bg-white rounded-lg shadow-lg p-6">
        <h2 class="text-2xl font-bold text-gray-800 mb-4 text-center">Post Details</h2>

        <!-- Post ID -->
        <div class="mb-4">
            <p class="text-sm text-gray-500"><strong>Post ID:</strong> <%= post.id %></p>
        </div>

        <!-- Username -->
        <div class="mb-4">
            <h3 class="text-lg font-medium text-gray-700">@<%= post.username %></h3>
        </div>

        <!-- Post Content -->
        <div class="mb-6">
            <h5 class="text-sm font-medium text-gray-600 mb-1">Content:</h5>
            <p class="text-gray-700 text-base"><%= post.content %></p>
        </div>

        <!-- Back Button -->
        <div class="text-center">
            <a href="/posts" class="bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-600">Back to Posts</a>
        </div>
    </div>

</body>
</html>
